import * as React from "react";
import styled from "styled-components";

const TitleStrong = styled.strong`
  font-size: 3em;
  line-height: 1.5em;
  text-transform: uppercase;
`;

export default TitleStrong;
